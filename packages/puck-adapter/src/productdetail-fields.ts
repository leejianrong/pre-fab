import type { Fields } from "@puckeditor/core";
import type { ProductDetailProps } from "@prefab/blocks";

/** No configurable props — see @prefab/blocks/productdetail/schema.ts. */
export const productDetailFields: Fields<ProductDetailProps> = {};
