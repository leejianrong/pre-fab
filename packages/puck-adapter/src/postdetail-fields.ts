import type { Fields } from "@puckeditor/core";
import type { PostDetailProps } from "@prefab/blocks";

/** No configurable props — see @prefab/blocks/postdetail/schema.ts. */
export const postDetailFields: Fields<PostDetailProps> = {};
